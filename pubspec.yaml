name: well_formed
description: A form fields package designed to relieve developers of much of the burden of form-related coding. It provides field masking, validation, smart trimming, and more.
version: 0.4.4
homepage: https://dartoos.dev
repository: https://github.com/dartoos-dev/well_formed

environment:
  sdk: ^3.5.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  formdator: ^1.1.1
  mask_text_input_formatter: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^2.3.0
